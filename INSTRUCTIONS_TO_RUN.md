# Instructions to Run SpeedX Bike Palace

## Prerequisites
- A modern web browser (Chrome, Firefox, Edge, Safari).
- No server is required; the site runs as a static HTML/JS/CSS project.

## Steps
1. Open the project folder:
   ```
   C:/Users/tina/Downloads/Projects for Evaluation-20260514T094413Z-3-001/Projects for Evaluation/speedx bike palace/speedx bike palace (bhanu and deeksha)
   ```
2. Double‑click `index.html` to launch the web application in your default browser.
3. Interact with the UI:
   - Use the navigation buttons to scroll to **Home**, **Rentals**, **Sell**, **Reviews**, etc.
   - Click a bike image to open its details modal.
   - Log in via **Account → Login** (default user: `user` / `user123`).
   - Admin login using **Account → Admin Login** (username: `admin`, password: `admin123`).
   - Add bikes to the cart, manage the cart, and proceed to checkout.
   - Use the **Filter** button to filter bikes by brand or type.
   - Add bikes to the wishlist via the heart icon that appears on each bike tile.
4. To edit code, open `index.html`, `style.css`, or any additional script files in a code editor.
5. Refresh the browser after making changes to see updates.

## Optional: Serve via a Local Development Server
If you prefer to serve the files over HTTP (e.g., to avoid CORS issues with local file URLs), you can use a simple Python server:
```bash
cd "C:/Users/tina/Downloads/Projects for Evaluation-20260514T094413Z-3-001/Projects for Evaluation/speedx bike palace/speedx bike palace (bhanu and deeksha)"
python -m http.server 8000
```
Then open `http://localhost:8000/index.html` in your browser.

---
*Generated by Antigravity AI.*
